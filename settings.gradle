pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        exclusiveContent {
            forRepository {
                maven {
                    name = 'Fabric'
                    url = uri('https://maven.fabricmc.net')
                }
            }
            filter {
                includeGroup('net.fabricmc')
                includeGroup('net.fabricmc.unpick')
                includeGroup('fabric-loom')
            }
        }
        exclusiveContent {
            forRepository {
                maven {
                    name = 'Sponge'
                    url = uri('https://repo.spongepowered.org/repository/maven-public')
                }
            }
            filter {
                includeGroupAndSubgroups("org.spongepowered")
            }
        }
        maven {
            name = 'NeoForged'
            url = uri('https://maven.neoforged.net/releases')
        }
        maven {
            name = 'QuiltMC'
            url = uri('https://maven.quiltmc.org/repository/release')
        }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
    id 'fabric-loom' version '1.15-SNAPSHOT' apply false
    id 'net.neoforged.moddev' version '2.0.87' apply false
}

// Force snakeyaml to resolve from Maven Central, not libraries.minecraft.net
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        exclusiveContent {
            forRepository {
                mavenCentral()
            }
            filter {
                includeGroup('org.yaml')
            }
        }
        maven {
            name = 'AliyunMirror'
            url = uri('https://maven.aliyun.com/repository/public')
        }
        maven {
            name = 'FabricMC'
            url = uri('https://maven.fabricmc.net')
        }
        maven {
            name = 'MinecraftLibraries'
            url = uri('https://libraries.minecraft.net')
        }
        maven {
            name = 'NeoForged'
            url = uri('https://maven.neoforged.net/releases')
        }
        exclusiveContent {
            forRepository {
                maven {
                    name = 'QuiltMC'
                    url = uri('https://maven.quiltmc.org/repository/release')
                }
            }
            filter {
                includeGroupAndSubgroups('org.quiltmc')
            }
        }
    }
}

// This should match the folder name of the project, or else IDEA may complain (see https://youtrack.jetbrains.com/issue/IDEA-317606)
rootProject.name = "Tpa"
include("common")
include("fabric")
//include("neoforge") // NeoForge 21.11 beta artifact 存在 ZIP 损坏问题，暂时禁用
include("quilt")